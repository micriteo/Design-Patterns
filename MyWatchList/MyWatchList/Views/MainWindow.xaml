<Window
    x:Class="MyWatchList.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyWatchList"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#990505">
        <Grid.RowDefinitions>
            <!-- Define the top row to be auto-sized (fits content) -->
            <RowDefinition Height="Auto"/>
            <!-- Define the bottom row to take up the remaining space -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Display the title "WatchList" with custom styling -->
        <TextBlock Text="WatchList" FontSize="50" Margin="10" FontFamily="Assets/Fonts/#Jockey One"/>

        <Rectangle Grid.Row="1" Width="2000" Height="4" Fill="White" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,0,0"/>

        <StackPanel Grid.Row="1" Margin="50">
            <TextBlock Text="Action" FontSize="30" Margin="100,10,0,0" FontFamily="Assets/Fonts/#Jockey One" HorizontalAlignment="Left"/>

            <Rectangle Grid.Row="1" Width="2000" Height="4" Fill="White" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="100,0,0,0"/>
            <!-- Wrap the ListView in a ScrollViewer -->
            <ScrollViewer HorizontalScrollBarVisibility="Visible">
                <ListView x:Name="ActionListView" Margin="30,30,0,0">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="50,0,0,0">
                                <Image Source="{Binding ImageSource}" Width="80" Height="60"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <!-- "Edit" button -->
                <Button Content="Edit" Margin="10,0,0,0" HorizontalAlignment="Right"/>
                <!-- "Delete" button -->
                <Button Content="Delete" Margin="10,0,0,0" HorizontalAlignment="Right"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>