<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MyWatchList.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyWatchList"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#990505">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="WatchList" FontSize="50" Margin="10" FontFamily="Jockey One"/>

        <Rectangle Grid.Row="0" Width="2000" Height="5" Fill="White" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>


        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="AddButton" Content="+"/>
            <Button x:Name="EditButton" Content="Edit"/>
            <Button x:Name="DeleteButton" Content="Delete"/>
        </StackPanel>

        <ListView Grid.Column="1" Grid.Row="0" Name="WatchListView" FontFamily="Jockey One">
        </ListView>
    </Grid>
</Window>
