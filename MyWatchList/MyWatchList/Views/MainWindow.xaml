<Window
    x:Class="MyWatchList.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyWatchList"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#990505">
        <Grid.RowDefinitions>
            <!-- Define the top row to be auto-sized (fits content) -->
            <RowDefinition Height="Auto"/>
            <!-- Define the bottom row to take up the remaining space -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ListView x:Name="ActionListView" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding ImageSource}" Width="80" Height="60"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Display the title "WatchList" with custom styling -->
        <TextBlock Text="WatchList" FontSize="50" Margin="10" FontFamily="Assets/Fonts/#Jockey One"/>

        <!-- Create a horizontal stack panel for buttons on the top right -->

        <!-- Add a button for editing items -->
        <Button x:Name="EditButton" Content="Edit" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,100,90,0"/>
        <!-- Add a button for deleting items -->
        <Button x:Name="DeleteButton" Content="Delete" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,100,10,0"/>


        <!-- Add a white horizontal line at the bottom of the title -->
        <Rectangle Grid.Row="0" Width="2000" Height="5" Fill="White" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>

        <!-- Display the "Action" text -->
        <TextBlock Text="Action" FontSize="30" Margin="100" FontFamily="Assets/Fonts/#Jockey One" HorizontalAlignment="Left" Grid.Row="1"/>

        <!-- Add a white horizontal line directly under the "Action" text -->
        <Rectangle Grid.Row="1" Width="2000" Height="4" Fill="White" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="100,140,0,0"/>
    </Grid>
</Window>