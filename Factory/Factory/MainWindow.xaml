<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Factory.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Factory"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ListView x:Name="yourGridView">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Watchable">
                    <StackPanel Orientation="Vertical">
                        <Image Source="{x:Bind ImageUrl}" Height="100" Width="100"/>
                        <TextBlock Text="{x:Bind Name}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind Description}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button x:Name="myButton" Content="Load Data" Click="myButton_Click"/>
    </Grid>
</Window>
